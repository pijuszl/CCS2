# Copy to .env and fill in if needed.
# The labelling agent runs as Claude Code in-the-loop (no API key needed).
# These are reserved for future automation.

# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=

# Override default cache / output locations.
HF_HOME=
TRANSFORMERS_CACHE=
TORCH_HOME=

# For Colab: override DATA_DIR / RESULTS_DIR to Google Drive mounts.
DATA_DIR=
RESULTS_DIR=
